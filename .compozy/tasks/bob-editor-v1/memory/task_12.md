# Task Memory: task_12.md

Keep only task-local execution context here. Do not duplicate facts that are obvious from the repository, task file, PRD documents, or git history.

## Objective Snapshot

Implement all 15 playground scenario routes + Playwright E2E on Chromium, Firefox, WebKit. MSW for image upload. `@axe-core/playwright` a11y scan per route.

## Important Decisions

- Used `vite preview` (not `vite dev`) for Playwright webServer — avoids EMFILE inotify watch limit on Linux.
- MSW must be enabled at BUILD time via `VITE_MSW=true`; baked into the `vite build` output.
- Hash-based router in App.tsx (no react-router-dom) — `window.location.hash`.
- WebKit lib workaround: manually placed `libavif13`, `libgav1-0`, `libyuv0`, `libevent-2.1-7` into `~/.cache/ms-playwright/webkit-*/minibrowser-{gtk,wpe}/lib/`. `PLAYWRIGHT_SKIP_VALIDATE_HOST_REQUIREMENTS=1` in playwright.config.ts.
- `typeInEditor` helper: detects WebKit via user-agent and uses `Meta+A` (not `Ctrl+A`) for select-all.
- Storage/uncontrolled scenarios: removed `defaultValue` prop so storage restore works on mount.
- E2E storage tests: navigate-away-and-back instead of `page.reload()` (Firefox timing issue).
- 1 test skipped intentionally: `Ctrl+K insert link dialog` — dialog not yet implemented in library.

## Learnings

- `useStorageSync` skips mount read when `defaultValue` provided (`if (hasDefaultValue) return` line 50). Promoted to shared memory.
- Debounce auto-save cancels on unmount — must wait ≥1500ms before toggle. Promoted to shared memory.
- Monaco `Ctrl+A` fails in WebKit — use `Meta+A`. Promoted to shared memory.
- `waitForEditor()` must prefer Monaco (6s wait) — TextareaFallback briefly visible as Suspense fallback.

## Files / Surfaces

- `apps/playground/src/App.tsx` — hash router + DevTools sidebar
- `apps/playground/src/scenarios/` — 15 scenario files
- `apps/playground/src/mocks/handlers.ts` + `browser.ts` — MSW handlers
- `apps/playground/e2e/` — Playwright specs + helpers
- `apps/playground/playwright.config.ts` — 3 browsers, vite preview, SKIP_VALIDATE env
- `apps/playground/public/mockServiceWorker.js` — generated by `msw init`
- `apps/playground/tests/scenarios.smoke.test.tsx` — RTL smoke renders (15/15)
- `packages/bob-editor/tests/fixtures/markdown/large.md` — 10k-line fixture

## Errors / Corrections

- Vite dev EMFILE: `vite dev` failed with too many inotify watches → switched to `vite preview`.
- `useStorageSync` + `defaultValue`: Uncontrolled and StorageScenario had `defaultValue` preventing restore → removed.
- WebKit exit 127: MiniBrowser needs bundled system libs not present on host → placed manually from apt packages.
- WebKit `Ctrl+A`: selects to line start in Monaco on WebKit → detect UA and use `Meta+A`.
- Default scenario h1 strict mode: two `<h1>` in preview due to existing content → added `exact: true` matcher.

## Status

DONE — 201/201 E2E pass (3 expected skips), 15/15 smoke pass, TypeScript clean.
