# Task Memory: task_01.md

Keep only task-local execution context here. Do not duplicate facts that are obvious from the repository, task file, PRD documents, or git history.

## Objective Snapshot

Bootstrap repo as pnpm workspace monorepo: root package.json, pnpm-workspace.yaml, .gitignore, .editorconfig, .nvmrc, LICENSE, pnpm-lock.yaml, scripts/smoke-workspaces.sh.

## Important Decisions

- Used pnpm@9.15.4 as the concrete version for packageManager field (task spec said "pnpm@9.x.x" as template). Tests require `startsWith('pnpm@9')`.
- Local pnpm is 10.14.0 but COREPACK_ENABLE_STRICT=0, so pnpm 10 runs install and generates lockfileVersion '9.0' (same format as pnpm 9). Lockfile is compatible.
- Smoke test uses `export WS_JSON` before `node -e` to pass shell var to node process.env — direct `WS_JSON="$VAL"` arg after `node -e` does not work.
- `node -e` inherits CJS mode even with `"type": "module"` in package.json, so `require()` works inside node -e scripts.

## Learnings

- pnpm 9 and pnpm 10 both use lockfileVersion '9.0' — no format incompatibility.
- `pnpm m ls --json` returns array with 1 root entry and 0 non-root entries when no workspace packages exist yet.

## Files / Surfaces

- `package.json` — root manifest
- `pnpm-workspace.yaml` — workspace globs: packages/_, apps/_
- `.gitignore`, `.editorconfig`, `.nvmrc`, `LICENSE` — hygiene files
- `pnpm-lock.yaml` — lockfileVersion '9.0', generated by pnpm@10.14.0
- `scripts/smoke-workspaces.sh` — 3-check smoke test (packageManager, ws discovery, frozen install)

## Errors / Corrections

- First attempt at smoke test used `WS_JSON="$VAL"` as argument after `node -e "..."` — this passes it as a node argument, not env var. Fixed with `export WS_JSON` before the node call.

## Ready for Next Run

Status: completed. All 7 deliverables created, all smoke checks pass, lockfile committed. task_02 (tsconfig.base + Turborepo) can proceed.
